void reverseString(char* s, int sSize){
    int i;
    char t;
    for(i = 0; i<sSize/2; i++)//i 不能 =sSize/2
    {
        t = s[i];
        s[i] = s[sSize-1-i];
        s[sSize-1-i] = t;
    }
    printf("%s",s);
}
